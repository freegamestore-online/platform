# @freegamestore/cli

## 0.1.18

- Add 5 missing templates to `fgs init`: cards, kaplay, phaser, pixi, babylon (8 total).

## 0.1.0

- Initial release. Commands: `login`, `logout`, `whoami`, `init`, `publish`, `check`, `logs`, `doctor`, `list`, `screencheck`.
